# Aegis — Python dependencies
# All versions pinned for SBOM reproducibility and supply-chain security.
# Update via: pip-compile requirements.in  (or manually after testing).
# Generated for: Aegis v2.5.0 / Python 3.11+

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.12
starlette==0.41.3               # pinned separately — FastAPI transitive dep (0.41.x required by fastapi 0.115.6)

# ── JWT / OIDC ────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
cryptography==46.0.5

# ── HTTP client ───────────────────────────────────────────────────────────────
requests==2.32.3
httpx==0.28.1                    # async-capable HTTP client
certifi==2024.12.14              # CA bundle pinned for supply-chain trust

# ── Data validation / config ──────────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# ── Redis ─────────────────────────────────────────────────────────────────────
redis==7.0.1

# ── Cloud SDKs ────────────────────────────────────────────────────────────────
boto3==1.35.95
botocore==1.35.95
s3transfer==0.10.4               # boto3 transitive dep — pinned to avoid drift

# Azure (uncomment when AZURE_ENABLED=true)
# azure-identity==1.19.0
# azure-mgmt-network==25.4.0
# azure-mgmt-storage==21.2.0

# GCP (uncomment when GCP_ENABLED=true)
# google-cloud-compute==1.19.3
# google-auth==2.37.0

# ── AI / LLM ──────────────────────────────────────────────────────────────────
openai==1.57.4                   # works with Ollama, vLLM, Groq, LocalAI, etc.
anthropic==0.45.0                # v3.0: Anthropic Claude connector

# ── AI Telemetry (v3.0) ──────────────────────────────────────────────────────
clickhouse-driver==0.2.9         # ClickHouse native protocol client

# ── Search / Analytics ────────────────────────────────────────────────────────
# Elasticsearch/OpenSearch REMOVED — all analytics now via ClickHouse (clickhouse-driver above)
# opensearch-py==2.7.1           # REMOVED in v4.0: migrated to ClickHouse
# elasticsearch==8.17.0          # REMOVED in v4.0: migrated to ClickHouse

# ── Network scanning (requires nmap binary: apt install nmap) ─────────────────
# python-nmap==0.7.1             # uncomment when NETWORK_SCAN_ENABLED=true

# ── Live network flow monitoring ────────────────────────────────────────────
psutil==6.1.1                    # cross-platform process & network connection info (BSD)

# ── Host malware scanning ───────────────────────────────────────────────────
yara-python==4.5.4               # YARA rule engine for malware pattern matching (Apache 2.0)
watchdog==6.0.0                  # filesystem event watcher for real-time download scanning

# ── Kubernetes scanning ───────────────────────────────────────────────────────
# kubernetes==31.0.0             # uncomment when K8S_ENABLED=true

# ── Utilities ─────────────────────────────────────────────────────────────────
anyio==4.7.0                     # FastAPI async runtime dep
click==8.1.8                     # uvicorn dep
typing_extensions==4.12.2        # backport — pinned to avoid version conflicts

# ── ACAS / Nessus scanner ─────────────────────────────────────────────────────
# No additional dependencies — ACASScanner uses Python stdlib only:
#   urllib.request, xml.etree.ElementTree, ssl, json, glob, re, dataclasses
